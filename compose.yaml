services:
  node_app:
    build: .
    networks:
      - app_network

  caddy:
    image: caddy:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    networks:
      - app_network

networks:
  app_network:
